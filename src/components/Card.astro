---
const { className = "" } = Astro.props as {
    className: string;
};
---

<div
    class={`p-4 bg-linear-to-t from-white/5 to-white/20 backdrop-blur rounded-md shadow-md w-full ${className}`}
>
    <slot />
</div>
